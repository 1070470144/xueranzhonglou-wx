# 任务清单：将返回按钮替换为 uni-app 自带回退按钮

**输入**：`specs/7-uni-back-btn/` 中的设计文档（spec.md、plan.md、research.md、quickstart.md）  
**组织原则**：按照用户故事（P1、P2）分组，每个故事独立可测

## 格式说明

- 任务格式：`- [ ] T### [P?] [US#] 描述（文件路径）`  
- `[P]` 表示可并行执行；Setup/Foundational/Polish 阶段不使用 Story 标签

---

## Phase 1：Setup（项目验证与环境准备）

- [x] T001 验证并切换到分支 `7-uni-back-btn`（仓库根目录）  
- [x] T002 确认 uni-app 开发环境与依赖安装就绪（检查 `xueran/package.json`，运行 `npm install` 如需）  
- [x] T003 审查当前实现：打开 `xueran/pages/detail/detail.vue` 并记录现有返回控件实现位置与样式

---

## Phase 2：Foundational（基础验证，阻塞所有故事）

- [x] T004 [P] 运行现有单元测试以建立基线（`cd xueran && npm test -- xueran/tests/pages/detail/detail.test.js`）  
- [x] T005 [P] 运行视觉回归基线截图脚本以保存当前 UI 状态（`tests/visual/baseline.spec.js`）  
- [x] T006 验证目标平台的 uni-icons 图标类型与 `uni.navigateBack()` 在 HBuilderX 与微信模拟器上可用（兼容性检查，记录差异）

**检查点**：基础验证完成后方可开始用户故事实现

---

## Phase 3：用户故事 1 - 使用内置回退控件（Priority: P1） 🎯 MVP

**目标**：将详情页的返回控件替换为 uni-app 内置实现，保证功能与无障碍性  
**独立测试**：点击返回控件触发 `uni.navigateBack()`，模板使用内置图标类型

### 测试任务（先写测试，TDD）

- [x] T007 [P] [US1] 在 `xueran/tests/pages/detail/detail.test.js` 中添加断言：存在内置图标类型且点击调用 `uni.navigateBack()`  

### 实现任务

- [x] T008 [US1] 在 `xueran/pages/detail/detail.vue` 中替换返回控件为 uni-app 内置实现（使用 `uni-icons` 内置类型或平台等效控件）  
- [x] T009 [US1] 在 `xueran/pages/detail/detail.vue` 中确保无障碍属性（`aria-label="返回"`、`role="button"`）存在并正确  
- [x] T010 [US1] 实现平台条件回退逻辑（如某平台图标不可用时选择兼容类型），修改位置：`xueran/pages/detail/detail.vue`  
- [x] T011 [US1] 更新组件注释与 `specs/7-uni-back-btn/quickstart.md`，说明使用内置实现与回退策略

**检查点**：US1 实现完成并通过单元测试与手动点击验证

---

## Phase 4：用户故事 2 - 视觉一致性与可见性（Priority: P2）

**目标**：保证内置控件在浅色/深色头图下均可辨识并与应用视觉一致  
**独立测试**：视觉回归比对通过（浅色/深色/无头图场景）

### 测试任务

- [x] T012 [P] [US2] 在 `tests/visual/baseline.spec.js` 中添加 detail 页返回控件截图任务（包含浅色/深色头图）  

### 实现任务

- [x] T013 [US2] 在 `xueran/pages/detail/detail.vue` 或全局样式中增加轻微填充/边框樣式以提高白色头部对比（参照 `specs/7-uni-back-btn/research.md`）  
- [ ] T014 [US2] 在 HBuilderX 与微信模拟器上进行跨平台视觉验证（记录差异，路径：`tests/visual/baseline-images/`）

**检查点**：US2 视觉基线更新并通过对比

---

## Phase 5：收尾与跨切关注点（Polish）

- [x] T015 更新 `specs/7-uni-back-btn/data-model.md` 与 `quickstart.md` 中的实现状态（文件路径）  
- [ ] T016 运行完整测试套件并修复可能的回归（`cd xueran && npm run test:run`）  
- [ ] T017 生成视觉基线截图并存放 `tests/visual/baseline-images/`（包含 detail-back-btn 场景）  
- [ ] T018 创建 Pull Request：分支 `7-uni-back-btn` → target branch，附带视觉差异截图与变更说明（PR 文本位置：仓库根或由开发者提交）

---

## 依赖与执行顺序

- Setup (Phase 1) 无依赖，可立即开始  
- Foundational (Phase 2) 必须在 Setup 完成后执行，阻塞用户故事实现  
- 用户故事（Phase 3/4）在 Foundational 完成后可并行或按优先级顺序执行（P1 优先）  
- 收尾（Phase 5）在所有用户故事完成后执行

## 并行机会

- 可同时运行 T004/T005（测试基线生成）  
- T007（单元测试编写）与 T012（视觉测试用例添加）可并行完成  
- 在 Foundational 完成后，US1 与 US2 的实现任务可由不同开发者并行执行（若团队允许）

## 输出

- 任务文件：`specs/7-uni-back-btn/tasks.md`（本文件）  
- 覆盖的实现文件：`xueran/pages/detail/detail.vue`、`xueran/tests/pages/detail/detail.test.js`、`tests/visual/baseline.spec.js`  
- 验证产物：视觉基线截图（`tests/visual/baseline-images/`）


